#this is an API Sandbox to test functionality - it has hardly any styling

I'm using node.js, with the dependencies listed in the Package.JSON file. 

The OMDB api is not public anymore, so I used an API key provided by a Colt Steele's
web developer bootcamp -  to get the results rendered. 